#pragma once
#include "InspectorDrawer.h"

// 인스펙터 커스텀 드로어 목록 (PHASE 21 W2-I).
//
// ── 포크한 개발자가 고치는 파일은 이것 하나다 ────────────────────────────
//
// 자기 값 타입에 커스텀 인스펙터를 붙이려면:
//   1. 자기 타입 옆에 `InspectorDrawer<T>` 특수화를 쓴다 (본보기는
//      `InspectorDrawerMath.h`, 규약은 `InspectorDrawer.h` 주석).
//   2. 아래에 그 헤더 include 를 한 줄 더한다.
//
// 엔진의 `if constexpr` 분기(`ReflectionTypedDraw.h`)는 건드리지 않는다.
// upstream 을 따라갈 때 충돌하는 자리가 이 파일의 include 목록뿐이다.
//
// ── 왜 목록이 필요한가 ──────────────────────────────────────────────────
//
// 특수화는 그것을 쓰는 번역 단위가 **보아야** 효력이 있다. 안 보이면 조용히
// 기본 위젯이 그려진다. 그래서 리플렉션 드로어가 이 파일 하나만 include 하고,
// 이 파일이 모든 특수화를 모은다.
//
// 자기 등록으로 목록을 없애는 길은 막혀 있다 — 엔진 모듈이 전부
// StaticLibrary 라 정적 초기화 등록자가 링크에서 떨어진다.

#include "InspectorDrawerMath.h"

// ── 포크 추가분은 이 아래에 ──────────────────────────────────────────────
