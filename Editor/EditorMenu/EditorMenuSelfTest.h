#pragma once
// 에디터 메뉴 배선 자가 검사 (PHASE 21 M0).
//
// 컴파일 타임 단정(표면 열거 완전성·문맥 타입 일치·이름 추출 카나리아)은 헤더에
// 이미 있다. 여기서 보는 것은 **선언이 실제로 표가 되는가**다 — 항목이 옳은
// 저장소에 들어갔는가, action_id가 함수 이름을 물고 왔는가, invoke 포인터가
// 실제로 그 함수를 부르는가, enabled 술어가 실려 왔는가.
//
// M2가 이것을 게이트에 잇는다. 그때 변이로 이빨을 증명한다.

#include <string>

namespace editor
{
    /// **아무 때나 돈다** — 제품 표를 옆으로 치우고(`stash_menu_registry`) 합성
    /// 선언 위에서 돌고 되돌린다. 조기 반환 경로에도 되돌리기가 달려 있다.
    ///
    /// M0 에서는 "표가 비어 있을 때만" 돌았고, 그래서 부팅 전 한순간에만 돌 수
    /// 있었다 — 곧 **도는 회귀 세트에 넣을 수 없다**는 뜻이었다. M1 이
    /// `EDITOR_MENU_LIST` 를 채우면서 그 전제가 깨졌고(M0 착지 기록이 예고한
    /// 대로다), 치우기·되돌리기를 달아 살아 있는 에디터에서 부를 수 있게 했다.
    /// 창 쪽 자가 검사가 M4 4단계에서 쓴 것과 같은 장치다.
    bool run_editor_menu_selftest(std::string& report);
}
