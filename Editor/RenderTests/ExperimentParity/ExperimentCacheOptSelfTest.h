#pragma once

#include <string>

namespace RenderTest
{
    // 정점 캐시/페치 최적화(I4)의 **합성** 검사. 자산을 읽지 않는다.
    //
    // ★ 용접에서 배운 것을 그대로 적용한다. 실자산은 이미 최적화된 채로
    //   내보내진 경우가 많아 개선폭이 0에 가까울 수 있고, 그러면 "이미 좋다"와
    //   "패스가 안 돈다"가 겉으로 같다. 일부러 캐시에 나쁜 순서를 만들어
    //   넣어야 이 패스가 살아 있는지 알 수 있다.
    //
    // ★ 그리고 속도보다 중요한 것이 **기하가 안 바뀌는가**이다. 삼각형 순서와
    //   정점 순서를 다시 쓰는 패스라, 재사상을 한 군데라도 빠뜨리면 모델이
    //   조용히 찢어진다. 그래서 인덱스가 아니라 **값으로** 삼각형 집합을
    //   비교한다.
    [[nodiscard]] bool RunExperimentCacheOptSelfTest(std::string& outLog);
}
