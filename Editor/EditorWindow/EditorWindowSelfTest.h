#pragma once
// 에디터 창 배선 자가 검사 (PHASE 21 M4 1단계).
//
// 컴파일 타임 단정(표면 열거 완전성·성질 번역 완전성·transient 도킹 금지)은
// 헤더와 셸에 이미 있다. 여기서 보는 것은 **선언이 실제로 표가 되는가**다 —
// 역할 기본값이 실려 왔는가, 수정자가 기본값을 덮는가, draw 포인터가 실제로
// 그 함수를 부르는가, available 술어가 실려 왔는가, 표시 상태가 항목에 붙어
// 있는가.
//
// 4단계가 이것을 게이트에 잇는다. 그때 변이로 이빨을 증명한다.

#include <string>

namespace editor
{
    /// 표가 **비어 있을 때만** 돈다 — 검사가 끝에서 표를 비우므로, 제품 등록
    /// 뒤에 부르면 등록분을 지운다. 비어 있지 않으면 거짓을 돌려주고 사유를 적는다.
    /// 그래서 부팅 순서는 자가 검사가 먼저이고 register_editor_windows()가 나중이다.
    bool run_editor_window_selftest(std::string& report);
}
