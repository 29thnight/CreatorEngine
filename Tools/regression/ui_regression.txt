# UI creation order regression: isolated scene and synthetic MeshRenderer owner.
# No external model or project asset is loaded or reimported.
# Structured UiTextProbe results must cover four play cycles.

wait 60
script.invoke UiTextProbe ResetResults
scene.new UIRegression
wait 30

object.create MeshCanvas Empty
component.add MeshCanvas MeshRenderer
wait 20

# 컴포넌트를 얹을 대상. 예전에는 기본 씬의 "Root"였다.
object.create Root Empty
wait 20

# 케이스 1: 캔버스 없이 UI 컴포넌트 먼저 (연결 실패 상태로 재생)
component.add Root TextComponent
wait 15
component.add Root ImageComponent
wait 15
play
wait 60
stop
wait 40

# 케이스 2: 캔버스를 나중에 추가 (지연 연결이 이어 줘야 함)
component.add Root Canvas
wait 15
script.add Root UiTextProbe
wait 20
play
wait 120
stop
wait 40

# 케이스 3: 빈 캔버스 단독 오브젝트 (6-0-② 재현 조건)
# — 3D 메시 오브젝트에 Canvas만 붙는다. UI 자식이 없다.
component.add MeshCanvas Canvas
wait 15
play
wait 60
stop
wait 40

# 케이스 4: 재생/정지 반복 (씬 사본 왕복에서 등록 누적·유실 확인)
play
wait 40
stop
wait 30
play
wait 40
stop
wait 40
script.invoke UiTextProbe Results
quit
